/target/
/_site/
# Keep local measurements out of Git; curated reports are already tracked.
/results/
# Local downloads, packed weights, and portable-root configuration.
/downloads/
/models/
/packed/
/scratch/
/cherenkov.toml
*.safetensors
*.bin
*.log
# macOS and local editor state. metal-lsp writes the root .clangd;
# kernels/.clangd is generated by `cargo xtask clangd` and tracked.
.DS_Store
/.clangd
!tests/fixtures/qwen4_exp/model.safetensors
/tests/fixtures/qwen4_exp/packed/
